# structureview
A clean document-style desktop viewer for Markdown and JSON — part of the FlowTrain toolchain. Built with Electron, with a VS Code extension companion planned. Renders AI structured outputs as readable documents.
