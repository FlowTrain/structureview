{
  "name": "@structureview/backend",
  "version": "0.1.0",
  "private": true,
  "description": "StructureView backend — auth, entitlements, billing webhooks",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "jest",
    "coverage": "jest --coverage",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "quality-gate": "npm run lint && npm run coverage"
  },
  "dependencies": {
    "fastify": "^4.26.0",
    "mongodb": "^7.2.0",
    "stripe": "^22.1.1",
    "@clerk/backend": "^3.4.7"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "prettier": "^3.2.5"
  }
}
